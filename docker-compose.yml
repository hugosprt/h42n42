services:
  h42n42:
    build:
      context: .
    ports:
      - "8080:8080"
    command: sh -lc "opam exec -- make all && opam exec -- ocsigenserver -c /app/h42n42.conf.in -v"
    volumes:
      - .:/app
